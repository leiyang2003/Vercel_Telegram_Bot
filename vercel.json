{
  "framework": null,
  "build": {
    "env": {
      "UV_SYSTEM_PYTHON": "1"
    }
  },
  "installCommand": "python -m pip install -U pip uv && uv pip install -r requirements.txt",
  "buildCommand": "echo \"no build step\""
}
